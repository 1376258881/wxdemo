.active{
  color: var(--themeColor);
  border-bottom: 5rpx solid var(--themeColor);
}
.tabs_title{
  display: flex;
}
.title_item{
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 15rpx 0;
}